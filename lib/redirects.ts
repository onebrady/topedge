/**
 * 301 Redirects Configuration
 *
 * Add your redirects here. Each redirect should have:
 * - source: The old URL path (can include wildcards)
 * - destination: The new URL path
 * - permanent: true for 301 redirects, false for 302
 *
 * Examples:
 *
 * Simple redirect:
 * { source: '/old-page', destination: '/new-page', permanent: true }
 *
 * Wildcard redirect:
 * { source: '/blog/:slug', destination: '/news/:slug', permanent: true }
 *
 * Multiple path segments:
 * { source: '/old/:path*', destination: '/new/:path*', permanent: true }
 */

export const redirects = [
  // Add your 301 redirects below this line
  // Example: { source: '/old-url', destination: '/new-url', permanent: true },
]
